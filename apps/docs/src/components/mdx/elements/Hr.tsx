import { styled } from 'styled-system/jsx';

const Hr = styled('hr', {
  base: {
    marginBlock: '24',
    border: 'none',
    borderTop: '1px solid',
    borderColor: 'layout.default-line',
  },
});

export default Hr;
